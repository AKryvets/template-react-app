// TODO delete this file(it was created for test)
export const actionsTypes = {
  TEST_ACTION: 'TEST_ACTION',
};

export const actions = {
  testAction: () => ({ type: actionsTypes.TEST_ACTION }),
};
